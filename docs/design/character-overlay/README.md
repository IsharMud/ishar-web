# Character overlay — verification proofs

Headless-Chromium screenshots of the merged Character overlay (worn gear +
stat sheet in one micro-menu app), rendered against the real `hud.js`/`hud.css`
with the `/connect?demo=1` sample feeds. See `docs/design/decisions.md`
(2026-07-19, "HUD Character overlay: merge worn gear + stats").

- `shot-desktop.png` — two columns with a divider (`#hud-overlay[data-app="train"]`).
- `shot-phone.png` — stacked in the bottom sheet at a true 390px width.
