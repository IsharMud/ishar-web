{% extends "layout.html.j2" %}
{% block title %}Portal{% endblock %}
{% block header %}Welcome{% if current_user.account_name is defined %}, {{ current_user.account_name }}{% endif %}!{% endblock %}
{%- block content %}
                {%- if current_user.players is defined and current_user.players is iterable and current_user.players|length > 0 %}
                <p>You have <strong>{{ current_user.players | length }}</strong> player{% if current_user.players|length != 1 %}s{% endif %}.</p>
                <table>
                    <tr>
                        <th>Name</th>
                        <th>Level</th>
                        <th>Remorts</th>
                        <th>Birth</th>
                    </tr>
                    {%- for player in current_user.players %}
                    <tr>
                        <td>{{ player.name }}</td>
                        <td>{{ player.level }}</td>
                        <td>{{ player.remorts }}</td>
                        <td>{{ player.birth | unix2human_time }}</td>
                    </tr>
                    {%- endfor %}
                </table>
                {%- else %}
                <p><strong>You have not created any players yet.</strong></p>
                {%- endif %}
{%- endblock %}
{%- if current_user.created_at is defined and current_user.created_at != '' %}
{% block footer %}
            <div id="created">You created your account on {{ current_user.created_at.strftime('%c') }}.</div>
{%- endblock %}
{%- endif %}
